# KNW-017 Nav Bar + Color Theme Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a responsive navigation bar (bottom on mobile, top on desktop) and change the primary color from black to sage green (#7a9e6a) across the entire app.

**Architecture:** New `components/nav.tsx` Client Component using `usePathname()` for active state. Added to `app/layout.tsx` with padding so pages don't overlap the nav. All black button/FAB colors replaced with green.

**Tech Stack:** Next.js 15 App Router, TypeScript, Tailwind CSS, lucide-react

---

## File Map

| File | Change |
|------|--------|
| `components/nav.tsx` | Create — global nav component |
| `app/layout.tsx` | Modify — add Nav, add pb-16 md:pt-14 to body |
| `app/people/page.tsx` | Modify — color replacements |
| `app/dashboard/page.tsx` | Modify — color replacements |
| `app/people/[id]/page.tsx` | Modify — color replacements |
| `app/new-person/page.tsx` | Modify — color replacements |

---

### Task 1: Create components/nav.tsx

**Files:**
- Create: `components/nav.tsx`

- [ ] **Step 1: Create the nav component**

Create `/Users/sha/Code/AI-code-26/5-konwho/components/nav.tsx` with the following content:

```tsx
"use client"

import Link from "next/link"
import { usePathname } from "next/navigation"
import { Home, Users } from "lucide-react"

const NAV_ITEMS = [
  { href: "/dashboard", label: "首页", icon: Home },
  { href: "/people", label: "联系人", icon: Users },
]

export function Nav() {
  const pathname = usePathname()

  return (
    <>
      {/* Mobile: bottom nav */}
      <nav className="fixed bottom-0 inset-x-0 h-16 bg-white border-t border-[#e8e0d8] flex items-center justify-around z-50 md:hidden">
        {NAV_ITEMS.map(({ href, label, icon: Icon }) => {
          const active = pathname === href || pathname.startsWith(href + "/")
          return (
            <Link
              key={href}
              href={href}
              className={`flex flex-col items-center gap-0.5 py-2 px-6 ${
                active ? "text-[#7a9e6a]" : "text-[#8b7d72]"
              }`}
            >
              <Icon className="h-5 w-5" />
              <span className="text-xs">{label}</span>
            </Link>
          )
        })}
      </nav>

      {/* Desktop: top nav */}
      <nav className="hidden md:flex fixed top-0 inset-x-0 h-14 bg-white border-b border-[#e8e0d8] items-center px-6 gap-6 z-50">
        <span className="font-bold text-[#2d2926] mr-4">Knowho</span>
        {NAV_ITEMS.map(({ href, label }) => {
          const active = pathname === href || pathname.startsWith(href + "/")
          return (
            <Link
              key={href}
              href={href}
              className={`text-sm ${
                active ? "text-[#7a9e6a] font-medium" : "text-[#8b7d72]"
              }`}
            >
              {label}
            </Link>
          )
        })}
      </nav>
    </>
  )
}
```

- [ ] **Step 2: Verify TypeScript**

```bash
cd /Users/sha/Code/AI-code-26/5-konwho && npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 3: Commit**

```bash
cd /Users/sha/Code/AI-code-26/5-konwho && git add components/nav.tsx && git commit -m "feat(knw-017): add responsive Nav component"
```

---

### Task 2: Update app/layout.tsx to include Nav

**Files:**
- Modify: `app/layout.tsx`

**Context:** The current layout is a simple server component. Nav is a Client Component so we import it directly — Next.js handles the boundary automatically.

- [ ] **Step 1: Update layout.tsx**

Replace the entire file content with:

```tsx
import type { Metadata } from "next"
import { Geist } from "next/font/google"
import "./globals.css"
import { Nav } from "@/components/nav"

const geist = Geist({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Knowho",
  description: "Remember everyone you meet.",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="zh">
      <body className={`${geist.className} pb-16 md:pt-14`}>
        <Nav />
        {children}
      </body>
    </html>
  )
}
```

- [ ] **Step 2: Verify TypeScript**

```bash
cd /Users/sha/Code/AI-code-26/5-konwho && npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 3: Commit**

```bash
cd /Users/sha/Code/AI-code-26/5-konwho && git add app/layout.tsx && git commit -m "feat(knw-017): add Nav to root layout with spacing"
```

---

### Task 3: Replace black colors with green in all pages

**Files:**
- Modify: `app/people/page.tsx`
- Modify: `app/dashboard/page.tsx`
- Modify: `app/people/[id]/page.tsx`
- Modify: `app/new-person/page.tsx`

**Context:** Read each file before editing. Do all four files in sequence then one commit.

The replacements to make in each file:
1. `bg-[#2d2926]` → `bg-[#7a9e6a]`
2. `hover:bg-[#3d3533]` → `hover:bg-[#6a8f5a]`

Do NOT change `text-[#2d2926]` — that's the main text color, keep it dark.

- [ ] **Step 1: Replace colors in app/people/page.tsx**

Read the file, then replace all occurrences of `bg-[#2d2926]` with `bg-[#7a9e6a]` and `hover:bg-[#3d3533]` with `hover:bg-[#6a8f5a]`.

- [ ] **Step 2: Replace colors in app/dashboard/page.tsx**

Read the file, then replace all occurrences of `bg-[#2d2926]` with `bg-[#7a9e6a]` and `hover:bg-[#3d3533]` with `hover:bg-[#6a8f5a]`.

- [ ] **Step 3: Replace colors in app/people/[id]/page.tsx**

Read the file, then replace all occurrences of `bg-[#2d2926]` with `bg-[#7a9e6a]` and `hover:bg-[#3d3533]` with `hover:bg-[#6a8f5a]`.

- [ ] **Step 4: Replace colors in app/new-person/page.tsx**

Read the file, then replace all occurrences of `bg-[#2d2926]` with `bg-[#7a9e6a]` and `hover:bg-[#3d3533]` with `hover:bg-[#6a8f5a]`.

- [ ] **Step 5: Verify TypeScript**

```bash
cd /Users/sha/Code/AI-code-26/5-konwho && npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 6: Verify no black button colors remain**

```bash
grep -rn "bg-\[#2d2926\]" /Users/sha/Code/AI-code-26/5-konwho/app --include="*.tsx" | grep -v ".next"
```

Expected: no output (all replaced).

- [ ] **Step 7: Commit**

```bash
cd /Users/sha/Code/AI-code-26/5-konwho && git add app/people/page.tsx app/dashboard/page.tsx "app/people/[id]/page.tsx" app/new-person/page.tsx && git commit -m "feat(knw-017): replace black button/FAB colors with sage green"
```
